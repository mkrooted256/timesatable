[build-system]
requires = ["setuptools>=70"]

[project]
name = "timesatable"
version = "0.1.1"
description = "Constraint-based timetable compiler using WBO SAT solver"
requires-python = "~=3.13.0"
dependencies = [
    "PyYAML>=6.0"
]

[project.scripts]
timesatable = "timesatable.cli:main"
